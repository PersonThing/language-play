{
  "name": "language-play-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^2.0.2",
    "gh-pages": "^5.0.0",
    "sass": "^1.57.1",
    "svelte": "^3.55.1",
    "svelte-preprocess": "^5.0.0",
    "svelte-routing": "^1.6.0",
    "vite": "^4.1.0"
  }
}
